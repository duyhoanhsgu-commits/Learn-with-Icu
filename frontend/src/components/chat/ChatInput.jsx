import { ArrowUp, Paperclip } from 'lucide-react'

export default function ChatInput({ value, onChange, onSubmit, disabled }) {
  const submit = (event) => { event.preventDefault(); onSubmit() }
  return <form onSubmit={submit} className="mx-auto w-full max-w-[770px] px-5 sm:px-0"><div className="flex items-end gap-2 rounded-2xl border border-slate-200 bg-white p-2 shadow-[0_8px_30px_rgba(15,23,42,0.07)] focus-within:border-teal/50 focus-within:ring-2 focus-within:ring-teal/10"><button type="button" aria-label="Attach a file" className="mb-0.5 rounded-xl p-2 text-slate-400 hover:bg-slate-100 hover:text-slate-600"><Paperclip size={19} /></button><textarea value={value} onChange={(e) => onChange(e.target.value)} onKeyDown={(e) => { if (e.key === 'Enter' && !e.shiftKey) { e.preventDefault(); if (value.trim() && !disabled) onSubmit() } }} rows="1" placeholder={'Ask anything — or say “quiz me on this”'} className="max-h-32 min-h-[40px] flex-1 resize-none border-0 bg-transparent px-1 py-2.5 text-sm text-ink outline-none placeholder:text-slate-400" /><button type="submit" aria-label="Send message" disabled={!value.trim() || disabled} className="mb-0.5 grid h-9 w-9 place-items-center rounded-xl bg-navy text-white transition hover:bg-teal disabled:cursor-not-allowed disabled:bg-slate-200 disabled:text-slate-400"><ArrowUp size={18} strokeWidth={2.5} /></button></div><p className="mt-2 text-center text-[10px] text-slate-400">ICU Tutor can make mistakes. Check important information.</p></form>
}
