import { ArrowUp, Paperclip } from 'lucide-react'

export default function ChatInput({ value, onChange, onSubmit, disabled, placeholder = 'Ask ICU anything...', variant = 'default' }) {
  const submit = (event) => { event.preventDefault(); onSubmit() }
  const general = variant === 'general'
  return <form onSubmit={submit} className={`mx-auto w-full px-4 sm:px-6 ${general ? 'max-w-[820px]' : 'max-w-[760px]'}`}><div className={`icu-soft-shadow flex items-end gap-2 rounded-[18px] border bg-white p-2.5 transition duration-200 ${disabled ? 'border-line bg-slate-50' : `border-line focus-within:-translate-y-0.5 focus-within:ring-4 ${general ? 'focus-within:border-teal/50 focus-within:ring-teal/10' : 'focus-within:border-brandblue/50 focus-within:ring-brandblue/10'}`}`}><button type="button" aria-label="Attach a file" disabled={disabled} className="mb-0.5 rounded-xl p-2.5 text-muted transition hover:bg-slate-100 hover:text-ink disabled:cursor-not-allowed disabled:opacity-50"><Paperclip size={18} /></button><textarea disabled={disabled} value={value} onChange={(e) => onChange(e.target.value)} onKeyDown={(e) => { if (e.key === 'Enter' && !e.shiftKey) { e.preventDefault(); if (value.trim() && !disabled) onSubmit() } }} rows="1" placeholder={placeholder} className="max-h-32 min-h-[42px] flex-1 resize-none border-0 bg-transparent px-1 py-2.5 text-sm text-ink outline-none placeholder:text-muted/80 disabled:cursor-not-allowed" /><button type="submit" aria-label="Send message" disabled={!value.trim() || disabled} className={`${general ? 'icu-tutor-gradient' : 'icu-action-gradient'} mb-0.5 grid h-10 w-10 shrink-0 place-items-center rounded-full text-white shadow-[0_7px_18px_rgba(52,133,245,.25)] transition hover:-translate-y-0.5 hover:shadow-[0_9px_22px_rgba(52,133,245,.34)] disabled:translate-y-0 disabled:cursor-not-allowed disabled:bg-none disabled:bg-slate-200 disabled:text-slate-400 disabled:shadow-none`}><ArrowUp size={18} strokeWidth={2.5} /></button></div><p className="mt-2 text-center text-[9px] text-muted">ICU Tutor can make mistakes. Check important information.</p></form>
}
